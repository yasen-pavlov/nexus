# Go
/nexus
bin/
coverage.out
*.exe
*.exe~
*.dll
*.so
*.dylib

# Node
web/node_modules/
web/dist/

# The React SPA is copied here by Docker / goreleaser so `//go:embed` picks it up.
# Only the .gitkeep is tracked; the actual bundle is generated, never committed.
internal/api/static/*
!internal/api/static/.gitkeep

# Playwright MCP + screenshot artifacts from ad-hoc visual verification
.playwright-mcp/
web/.playwright-mcp/
web/test-results/
ranking-verify*.png

# Runtime: local binary cache for `make dev` / go run ./cmd/nexus
data/

# Environment
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
